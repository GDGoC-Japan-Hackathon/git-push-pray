* 可読性・保守性を最優先に実装する
* コンポーネントは責務ごとに適切に分割する（巨大コンポーネントを作らない）
* UIとロジックは可能な限り分離し、複雑なロジックは custom hook に切り出す
* 同じロジックを複数箇所に書かない（共通化する）
* 深いネストや複雑な条件分岐を避ける
* 型はできる限り明示し、`any` は使用しない
* コメントは日本語で、複雑な処理・状態管理・API通信には必ず記述する
* 不必要な再レンダリングや状態の持ちすぎを避ける
* 仕様・データ構造・APIなど不明点がある場合は、実装前に必ず質問する

